import styled from "styled-components";

const Title = styled.header`
  margin: 20px 0px;

  text-align: center;
  font-size: 30px;
  font-weight: 700;
` 

export default function Header() {
  return (
      <Title>
          나는 누구일까용?
      </Title>
  )
}
